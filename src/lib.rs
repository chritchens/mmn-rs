/// `result` is the module containing the crate `Result` type.
pub mod result;

/// `path` is the module containing the data paths.
pub mod path;

/// `data_entry` is the module containing the `DataEntry` type.
pub mod data_entry;

/// `data_entries` is the module containing the `DataEntries` type.
pub mod data_entries;

